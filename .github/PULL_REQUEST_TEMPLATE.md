## Release metadata

Select exactly one release classification label:

- [ ] `release:none`
- [ ] `release:patch`
- [ ] `release:minor`
- [ ] `release:major`
- [ ] `release:security`

User-facing summary (present tense):

Affected API, effect, browser/support, package, asset, or security scope:

Migration or deprecation guidance (write `None` when not applicable):

Asset/runtime size, loading, hosting, CSP/CORS, license, or integrity impact
(write `None` when not applicable):

Stable lifecycle, ownership, output, SSR, compatibility, or publication impact
(write `None` when not applicable):

Normal pull requests must not edit the current package version or released
changelog sections. The release PR generated by tagpr is the authoritative
place for those generated files.
